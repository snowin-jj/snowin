---
const { name, type, placeholder, tw } = Astro.props;
---

<input
  required
  type={type}
  name={name}
  id={name}
  placeholder={placeholder}
  class={`w-full rounded-lg py-2 px-2 md:py-3 md:px-4 text-primaryDark placeholder:text-white form-inputs ${tw}`}
/>
